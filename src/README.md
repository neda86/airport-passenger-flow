# src/

Drop your GAT-LSTM code here. Suggested layout:

```
src/
  model.py        # GAT encoder + LSTM head
  dataset.py      # loaders for the schema in data/README.md
  train.py        # training loop
  evaluate.py     # 3-hour-horizon metrics (MAE/RMSE per step)
```
Delete this file once real code is in place.
